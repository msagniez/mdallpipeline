// Global default params, used in configs
params {
    // TODO: Add your own default parameters here
    // -------------------------------------------------------------------------
    // Basecalling and mapping options
    // -------------------------------------------------------------------------
    basecall_model       = null   // Basecalling model (Dorado)

    // Pipeline-specific options
    // Input options
    input                        = null
    reference_dir                = null
    // MDALL parameters
    mdall_feature_n = "1000,1058"
    run_mdall = false
    // Module system (only used when modules are available)
    r_module = 'R/4.2.0'
    bioconductor_modules = 'DESeq2/1.38.0:SummarizedExperiment/1.28.0'
    // Boilerplate options
    outdir                       = './results'
    publish_dir_mode             = 'copy'
    igenomes_base                = 's3://ngi-igenomes/igenomes/'
    email                        = null
    email_on_fail                = null
    plaintext_email              = false
    monochrome_logs              = false
    hook_url                     = null
    help                         = false
    help_full                    = false
    show_hidden                  = false
    version                      = false
    pipelines_testdata_base_path = '/workspaces/nf-core-oncoseq/test_data/'
    trace_report_suffix          = new java.util.Date().format( 'yyyy-MM-dd_HH-mm-ss')// Config options
    config_profile_name          = null
    config_profile_description   = null
    custom_config_version        = 'master'
    custom_config_base           = "https://raw.githubusercontent.com/nf-core/configs/${params.custom_config_version}"
    config_profile_contact       = null
    config_profile_url           = null
    // Schema validation default options
    validate_params              = true
}